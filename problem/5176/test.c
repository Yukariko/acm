int p,m,n,s,i;
main(k)
{
  for(scanf("%d",&k);k--;printf("%d\n",p-s))
  {
    int seat[501]={0};
    scanf("%d%d",&p,&m);
    for(i=0;i++<p;seat[n]++)scanf("%d",&n);
    for(s=i=0;i<=m;i++)s+=seat[i]?1:0;
  }
}
